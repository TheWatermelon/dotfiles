
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# modifier : Mod1:Alt, Mod4:Win
set $mod4 Mod4
set $mod1 Mod1

# font for window titles. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# Mac-like font
font pango:SFNS Display 0

# Use Mouse+$mod4 to drag floating windows to their wanted position
floating_modifier $mod4

# Set window border
default_border pixel 2
default_floating_border pixel 2

# Hide borders when there is only 1 window in current workspace
hide_edge_borders smart

# Self-explanatory
popup_during_fullscreen smart

#
# Exec programs
#
set $compton "compton -f --fade-delta=3 &"
set $dmenu "$HOME/bin/dmenu_themed"
set $dunst "dunst -config $HOME/.cache/wal/dunst.config &"
set $i3exit "i3-nagbar -t warning -m 'You pressed the exit \
            shortcut. Do you really want to exit i3? This \
            will end your X session.' -b 'Yes, exit i3' \
            'i3-msg exit'"
set $pywal "wal -i images/wallpapers/minimalist"
set $rofi "rofi -modi drun,run,window -show drun -theme $HOME/.cache/wal/rofi_theme.rasi"
set $termite "termite -c $HOME/.cache/wal/termite.config"
set $unclutter "unclutter -b --timeout 3"
set $volup "pactl set-sink-volume 0 +5%"
set $voldown "pactl set-sink-volume 0 -5%"

#
# Colors
#

# Set colors from Xresources
# color[0-15] available
# NOTE: The '#f0f0f0' in the lines below is the color i3 will use if
# it fails to get colors from Xresources.
set_from_resource $color0 i3wm.color0 #f0f0f0
set_from_resource $color1 i3wm.color1 #f0f0f0
set_from_resource $color2 i3wm.color2 #f0f0f0
set_from_resource $color3 i3wm.color3 #f0f0f0
set_from_resource $color4 i3wm.color4 #f0f0f0
set_from_resource $color5 i3wm.color5 #f0f0f0
set_from_resource $color6 i3wm.color6 #f0f0f0
set_from_resource $color7 i3wm.color7 #f0f0f0
set_from_resource $color8 i3wm.color8 #f0f0f0
set_from_resource $color9 i3wm.color9 #f0f0f0
set_from_resource $color10 i3wm.color10 #f0f0f0
set_from_resource $color11 i3wm.color11 #f0f0f0
set_from_resource $color12 i3wm.color12 #f0f0f0
set_from_resource $color13 i3wm.color13 #f0f0f0
set_from_resource $color14 i3wm.color14 #f0f0f0
set_from_resource $color15 i3wm.color15 #f0f0f0
set $white #FFFFFF

# old theme
#set $skin-light #EA1646
#set $skin-main #C71340
#set $skin-dark #770B26
#set $foreground #DFDFDF
#set $foreground-alt #777
#set $background-light #2E1D36
#set $background-dark #170420
#set $alert #BD2C40

# class                 border     backgr.    text       indicator
client.focused          $color2    $color3    $white     $color3
client.focused_inactive $color3    $color4    $color7    $color3
client.unfocused        $color0    $color1    $color7    $color3
client.urgent           $color4    $color11   $white     $color3
#client.placeholder      #000000      #0c0c0c     #ffffff #000000

#
# Workspaces
#
set $workspace1 "1"
set $workspace2 "2"
set $workspace3 "3"
set $workspace4 "4"
set $workspace5 "5"
set $workspace6 "6"
set $workspace7 "7"
set $workspace8 "8"
set $workspace9 "9"
set $workspace10 "10"

# 
# Keybindings
#

#bindsym $mod4+Return
#bindsym $mod4+Shift+Return
## change focus between tiling / floating windows
bindsym $mod4+space focus mode_toggle 
## toggle tiling / floating
bindsym $mod4+Shift+space floating toggle 
## Go to next workspace
bindsym $mod1+Tab workspace next
bindsym $mod4+Next workspace next
## Go to previous workspace
bindsym $mod1+Shift+Tab workspace prev
bindsym $mod4+Prior workspace prev
## switch to workspace
bindsym $mod4+ampersand workspace $workspace1
bindsym $mod4+1 workspace $workspace1
bindsym $mod4+eacute workspace $workspace2
bindsym $mod4+2 workspace $workspace2
bindsym $mod4+quotedbl workspace $workspace3
bindsym $mod4+3 workspace $workspace3
bindsym $mod4+apostrophe workspace $workspace4
bindsym $mod4+4 workspace $workspace4
bindsym $mod4+parenleft workspace $workspace5
bindsym $mod4+5 workspace $workspace5
bindsym $mod4+minus workspace $workspace6
bindsym $mod4+6 workspace $workspace6
bindsym $mod4+egrave workspace $workspace7
bindsym $mod4+7 workspace $workspace7
bindsym $mod4+underscore workspace $workspace8
bindsym $mod4+8 workspace $workspace8
bindsym $mod4+ccedilla workspace $workspace9
bindsym $mod4+9 workspace $workspace9
bindsym $mod4+agrave workspace $workspace10
bindsym $mod4+0 workspace $workspace10
## move focused container to workspace
bindsym $mod4+Shift+1 move container to workspace $workspace1
bindsym $mod4+Shift+2 move container to workspace $workspace2
bindsym $mod4+Shift+3 move container to workspace $workspace3
bindsym $mod4+Shift+4 move container to workspace $workspace4
bindsym $mod4+Shift+5 move container to workspace $workspace5
bindsym $mod4+Shift+6 move container to workspace $workspace6
bindsym $mod4+Shift+7 move container to workspace $workspace7
bindsym $mod4+Shift+8 move container to workspace $workspace8
bindsym $mod4+Shift+9 move container to workspace $workspace9
bindsym $mod4+Shift+0 move container to workspace $workspace10
## change focus
bindsym $mod4+Left focus left
bindsym $mod4+Down focus down
bindsym $mod4+Up focus up
bindsym $mod4+Right focus right
## move focused window
bindsym $mod4+Shift+Left move left
bindsym $mod4+Shift+Down move down
bindsym $mod4+Shift+Up move up
bindsym $mod4+Shift+Right move right
## Brightness control
bindsym XF86MonBrightnessUp exec xbacklight -inc 1
bindsym XF86MonBrightnessDown exec xbacklight -dec 1
## Volume control
bindsym XF86AudioRaiseVolume exec $volup
bindsym XF86AudioLowerVolume exec $voldown

### Controls are ordered by azerty keyboard layout ###
## change container layout to stacked
bindsym $mod4+a layout stacking
#bindsym $mod4+Shift+a
## change container layout to tabbed 
bindsym $mod4+z layout tabbed
#bindsym $mod4+Shift+z
## set layout to default
bindsym $mod4+e layout default 
## exit i3
bindsym $mod4+Shift+e exec --no-startup-id $i3exit
   
## toggle resize mode
#bindsym $mod4+r mode "resize" 
## restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod4+Shift+R restart 
#bindsym $mod4+t
#bindsym $mod4+Shift+t
bindsym $mod4+y resize shrink width 2 px or 2 ppt
#bindsym $mod4+Shift+y
bindsym $mod4+u resize shrink height 2 px or 2 ppt
#bindsym $mod4+Shift+u
bindsym $mod4+i resize grow height 2 px or 2 ppt
#bindsym $mod4+Shift+i
bindsym $mod4+o resize grow width 2 px or 2 ppt
#bindsym $mod4+Shift+o
## start rofi (program launcher)
bindsym $mod4+p exec $rofi
## start dmenu (program launcher)
bindsym $mod4+Shift+p exec $dmenu
## kill focused window
bindsym $mod4+q kill 
#bindsym $mod4+Shift+q
#bindsym $mod4+s
#bindsym $mod4+Shift+s
## focus the child
bindsym $mod4+d focus child 
#bindsym $mod4+Shift+d
## set focused window on fullscreen
bindsym $mod4+f fullscreen 
## change background wallpaper
bindsym $mod4+Shift+f exec $pywal
## show/hide polybar
bindsym $mod4+g exec ~/bin/toggle_polybar.sh
#bindsym $mod4+Shift+g
## focus left
bindsym $mod4+h focus left 
## move focused window left
bindsym $mod4+Shift+h move left 
## focus down
bindsym $mod4+j focus down 
## move focused window down
bindsym $mod4+Shift+j move down 
## focus up
bindsym $mod4+k focus up 
## move focused window down
bindsym $mod4+Shift+k move up 
## focus right
bindsym $mod4+l focus right 
## move focused window right
bindsym $mod4+Shift+l move right 
#bindsym $mod4+m
#bindsym $mod4+Shift+m
## split in vertical orientation
bindsym $mod4+w split v
#bindsym $mod4+Shift+w
bindsym $mod4+x exec $termite
## lock the screen
#bindsym $mod4+Shift+X exec $HOME/bin/i3lock_random_color.sh 
## focus parent container
bindsym $mod4+c focus parent 
## split in horizontal orientation
bindsym $mod4+v split h
#bindsym $mod4+Shift+v
#bindsym $mod4+b 
#bindsym $mod4+Shift+b
#bindsym $mod4+n
#bindsym $mod4+Shift+n

#
# Modes
#

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: $mod4+r, Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

smart_gaps on
set $mode_gaps Gaps: (o)uter, (i)nner, [Esc] to quit
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod4+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym h      mode "$mode_gaps_horiz"
        bindsym v      mode "$mode_gaps_verti"
        bindsym t      mode "$mode_gaps_top"
        bindsym r      mode "$mode_gaps_right"
        bindsym b      mode "$mode_gaps_bottom"
        bindsym l      mode "$mode_gaps_left"
        bindsym Return mode "$mode_gaps"
        bindsym Escape mode "default"
}

mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "$mode_gaps"
        bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "$mode_gaps"
        bindsym Escape mode "default"
}

#
# Startup commands
#

# Start composite manager
exec $compton
# Gestures for the trackpad
exec libinput-gestures &
# Hide the cursor after 3 seconds
exec $unclutter
# Launch background and colorscheme selector
exec $pywal
# Start polybar
exec_always $HOME/bin/polybarstart
# Dunst notifications
exec $dunst
# Tweak x config using xinput
exec $HOME/bin/xinput_config
